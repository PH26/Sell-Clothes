MSG.SITE.CONTENT = {
    ERROR_LOADING_RELATION: "Have error when load relation contents"
};

URL.SITE.CONTENT = {
    GET_IMAGE: ROOT_URL + APP.getInfo('prefix', '/') + '/content/ajax/get-image',
    GET_RELATIVE_CONTENTS: ROOT_URL + APP.getInfo('prefix', '/') + '/content/ajax/get-relative-contents',
    CONFIRM_PASSWORD: ROOT_URL + APP.getInfo('prefix', '/') + '/content/ajax/check-password-update'
}