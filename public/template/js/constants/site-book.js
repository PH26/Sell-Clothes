MSG.SITE.BOOK = {

};

URL.SITE.BOOK = {
    BOOK_LIST: ROOT_URL + APP.getInfo('prefix', '/') + '/book/ajax/get-book-list',
    CONTENT_LIST: ROOT_URL + APP.getInfo('prefix', '/') + '/book/ajax/contents-by-book',
}