MSG.SITE.TOP = {

};

URL.SITE.HOME = {
    TOP_CONTENTS: ROOT_URL + APP.getInfo('prefix', '/') + '/home/ajax/contents',
    SEARCH_CONTENTS: ROOT_URL + APP.getInfo('prefix', '/') + '/search/ajax/contents'
};